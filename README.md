Per tutti gli esercizi tranne Sito_conversione_tra_basi, l'interfaccia e' stata progettata dal prof

Sito_conversione_tra_basi:
 La prima pagina contiene un convertitore di basi. La seconda contiene tutta la teoria fatta dal nostro prof fino alla creazione di questo sito

Gianni_Ye_hamming.html:
 L'esercizio consiste nell'implementare l'algoritmo di hamming su una sequanza di bit, inserita dall'utente, usando il linguaggio JS

colori_hex.html:
 L'esercizio consiste nell'implementare l'algoritmo della parita' dei bit su un immagine, creata dall'utente con i codici di colore hex, usando il linguaggio JS
